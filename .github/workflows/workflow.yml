name: Build & Test
on: [push]
jobs:
#   build:
#     name: Build
#     runs-on: ${{ matrix.operating-system }}
#     strategy:
#       matrix:
#         operating-system: [ubuntu-latest, windows-latest]
#     steps:
#     - name: Checkout
#       uses: actions/checkout@master

#     - name: Set Node.js 10.x
#       uses: actions/setup-node@master
#       with:
#         version: 10.x

#     - name: npm install
#       run: npm install

#     - name: Lint
#       run: npm run format-check

    # - name: npm test
    #   run: npm test
  test:
#     needs: build
    name: Test
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [windows-latest]
    steps:
    - name: Install hub
      uses: geertvdc/setup-hub@master
      
    - name: run hub
      run: |
           echo %PATH%
           dir "C:\hostedtoolcache\windows\hub"
           dir "C:\hostedtoolcache\windows\hub\2.12.3\x64"
           dir "C:\hostedtoolcache\windows\hub\2.12.3\x64\bin"
           hub --version
